<p id="notice"><%= notice %></p>
<center>
<div class="container">
  <center>
    <div class="row">
      <center>
        <div class="col-xs-12 col-sm-6 col-md-6" style="margin-left:200px">
            <div class="well well-sm">
                <div class="row">
                    <div class="col-sm-6 col-md-4">
                      <% if @current_user.image? %>
                        <%= image_tag @current_user.image, style:"height:140px" %>
                        <%= link_to @current_user.image.url, @current_user.image.url %>
                      <% end %>

                    </div>
                    <div class="col-sm-6 col-md-8">
                        <h4>
                          <%= @current_user.name %></h4>
                        <cite title="username">username:<%= @current_user.username %>
                        </i></cite>
                        <p>
                            <i class="glyphicon glyphicon-envelope"></i><%= @current_user.email %>
                            <br />
                          

                            <br />
                            </p>

                    </div>
                </div>
            </div>
        </div>
      </center>
    </div>
  </center>
</div>


<%= link_to 'Edit', edit_user_path(@current_user) %> |
<td><%= link_to 'Destroy', @current_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
<%= link_to 'Back', users_path %>
</center>
